?power.t.test

# combien d'animaux ?
power.t.test(sd=125, n=NULL, delta=100, sig.level =0.05, power = 0.99, 
             alternative = "one.sided")

# quelle puissance ?
power.t.test(sd=125, n=40, delta=100, sig.level =0.05, power = , 
             alternative = "one.sided")

# quelle difference je peux detecter ? delta ?
power.t.test(sd=125, n=40, delta=, sig.level =0.05, power = 0.9, 
             alternative = "one.sided")
